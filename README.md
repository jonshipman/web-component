# WordPress Plugin with Web Component

A quick boilerplate/demo of a svelte + vite web component integrated into a WordPress plugin. The web component is rendered from the shortcode `[web-component-demo]`.
